"""graphrag.ontologies._impl — bundled ontology definitions.

Concrete `Ontology` instances (e.g. `beiyin`) live here; they are exported as
constants by `graphrag.ontologies`. External code imports the constants from
`graphrag.ontologies`, not from this subpackage.
"""
